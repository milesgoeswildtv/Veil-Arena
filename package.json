{
  "name": "veil-arena",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:activity-client": "node -e \"console.log('Veil Arena clean baseline: no Activity client to build yet.')\"",
    "build": "npm run build:activity-client",
    "test": "node scripts/smoke-test.mjs && node scripts/full-tilt-copy-smoke-test.mjs && node scripts/sponsorship-smoke-test.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
