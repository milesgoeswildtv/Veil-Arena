{
  "name": "veil-arena",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node scripts/smoke-test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
