{
  "name": "veil-arena",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:discord-sdk": "node scripts/build-discord-sdk.mjs",
    "postinstall": "npm run build:discord-sdk",
    "deploy": "npm run build:discord-sdk && wrangler deploy",
    "deploy:test": "npm run build:discord-sdk && wrangler deploy --env test",
    "dev": "npm run build:discord-sdk && wrangler dev",
    "dev:test": "npm run build:discord-sdk && wrangler dev --env test",
    "test": "npm run build:discord-sdk && node scripts/smoke-test.mjs && node scripts/telegram-smoke-test.mjs && node scripts/sponsorship-smoke-test.mjs && node scripts/test-mode-smoke-test.mjs && node scripts/full-tilt-copy-smoke-test.mjs && node scripts/activity-live-smoke-test.mjs"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "2.5.0",
    "esbuild": "^0.25.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
