{
  "name": "veil-arena",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:test": "wrangler deploy --env test",
    "dev": "wrangler dev",
    "dev:test": "wrangler dev --env test",
    "test": "node scripts/smoke-test.mjs && node scripts/telegram-smoke-test.mjs && node scripts/sponsorship-smoke-test.mjs && node scripts/test-mode-smoke-test.mjs && node scripts/full-tilt-copy-smoke-test.mjs && node scripts/activity-live-smoke-test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
